<!DOCTYPE html>
<html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>{{ $thread->title }} · Mundo Yuri</title><x-portal-favicon /><link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('assets/css/style.css') }}?v={{ filemtime(public_path('assets/css/style.css')) }}"></head>
<body><x-navbar /><main class="portal-profile-page forum-page"><div class="container-xl px-4 position-relative"><nav class="profile-breadcrumb"><a href="{{ route('forums.index') }}">Foros</a><span>›</span><a href="{{ route('forums.show', $thread->forum) }}">{{ $thread->forum->name }}</a><span>›</span><span>{{ $thread->title }}</span></nav><header class="forum-thread-header"><div><span class="profile-eyebrow">Tema de {{ $thread->forum->name }}</span><h1>{{ $thread->title }}</h1><p>Creado por @if($thread->author)<a href="{{ $thread->author->publicProfileUrl() }}">{{ $thread->author->displayName() }}</a>@else{{ $thread->authorName() }}@endif · {{ number_format($thread->views_count) }} vistas</p></div><div class="forum-thread-header-actions">@auth @if($isSubscribed)<form method="POST" action="{{ route('forum.subscriptions.destroy', $thread) }}">@csrf @method('DELETE')<button class="profile-btn profile-btn-soft" type="submit">Dejar de seguir</button></form>@else<form method="POST" action="{{ route('forum.subscriptions.store', $thread) }}">@csrf <button class="profile-btn profile-btn-soft" type="submit">Seguir tema</button></form>@endif @can('update', $thread)<a href="{{ route('forum.threads.edit', $thread) }}" class="profile-btn profile-btn-soft">Editar</a>@endcan @endauth</div></header>
@if(session('success'))<div class="legacy-profile-notice mt-3"><strong>Listo</strong><span>{{ session('success') }}</span></div>@endif
<section class="forum-posts">@php($previousUserId = null)@foreach($posts as $post)<x-forum.post :post="$post" :previous-user-id="$previousUserId" />@php($previousUserId = $post->user_id)@endforeach</section>{{ $posts->links() }}
@auth @can('reply', $thread)<section class="forum-reply-section"><h2>Responder</h2><x-forum.composer :action="route('forum.posts.store', $thread)" submit="Publicar respuesta" /></section>@else <section class="profile-panel forum-readonly"><p>{{ $thread->is_locked || $thread->forum->is_locked ? 'Este tema está cerrado para nuevas respuestas.' : 'No tienes permiso para responder en este foro.' }}</p></section>@endcan @else <section class="profile-panel forum-readonly"><p><a href="{{ route('login') }}">Inicia sesión</a> para participar en la conversación.</p></section>@endauth
@auth @can('moderate', $thread)<section class="profile-panel forum-moderation"><h2>Moderación</h2><form method="POST" action="{{ route('forum.moderation.thread.update', $thread) }}">@csrf @method('PATCH')<input type="hidden" name="is_locked" value="0"><label><input type="checkbox" name="is_locked" value="1" @checked($thread->is_locked)> Cerrar tema</label><input type="hidden" name="is_pinned" value="0"><label><input type="checkbox" name="is_pinned" value="1" @checked($thread->is_pinned)> Fijar tema</label><input type="hidden" name="is_hidden" value="0"><label><input type="checkbox" name="is_hidden" value="1" @checked($thread->is_hidden)> Ocultar tema</label><label>Mover a <select name="forum_id">@foreach($moderationForums as $moderationForum)<option value="{{ $moderationForum->id }}" @selected($thread->forum_id === $moderationForum->id)>{{ $moderationForum->category->name }} · {{ $moderationForum->name }}</option>@endforeach</select></label><button class="profile-btn profile-btn-soft" type="submit">Guardar moderación</button></form></section>@endcan @endauth
</div></main><x-footer /></body></html>
