<!DOCTYPE html>
<html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>{{ $forum->name }} · Foros · Mundo Yuri</title><x-portal-favicon /><link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('assets/css/style.css') }}?v={{ filemtime(public_path('assets/css/style.css')) }}"></head>
<body><x-navbar /><main class="portal-profile-page forum-page"><div class="container-xl px-4 position-relative"><nav class="profile-breadcrumb"><a href="{{ route('forums.index') }}">Foros</a><span>›</span><span>{{ $forum->name }}</span></nav><header class="forum-forum-header"><div><span class="profile-eyebrow">{{ $forum->category->name }}</span><h1>{{ $forum->name }}</h1><p>{{ $forum->description }}</p></div>@auth @can('createTopic', $forum)<a class="profile-btn profile-btn-primary" href="{{ route('forum.threads.create', $forum) }}">Crear tema</a>@endcan @endauth</header><form method="GET" class="forum-search forum-search-inline"><input name="q" type="search" value="{{ $search }}" placeholder="Buscar temas"><button type="submit">Buscar</button></form><section class="forum-thread-list">@forelse($threads as $thread)<article class="forum-thread-row"><div class="forum-thread-status">{{ $thread->is_pinned ? '★' : ($thread->is_locked ? '◌' : '◍') }}</div><div class="forum-thread-main"><h2><a href="{{ route('forum.threads.show', $thread) }}">{{ $thread->title }}</a>@if($thread->is_pinned)<span class="forum-state">Fijado</span>@endif</h2><p>Por @if($thread->author)<a href="{{ $thread->author->publicProfileUrl() }}">{{ $thread->author->displayName() }}</a>@else{{ $thread->authorName() }}@endif · {{ $thread->created_at->diffForHumans() }}</p></div><dl><div><dt>Respuestas</dt><dd>{{ number_format($thread->replies_count) }}</dd></div><div><dt>Vistas</dt><dd>{{ number_format($thread->views_count) }}</dd></div></dl><div class="forum-row-last">@if($thread->latestVisiblePost)<span>Última respuesta</span><a href="{{ route('forum.threads.show', $thread) }}#post-{{ $thread->latestVisiblePost->id }}">{{ $thread->latestVisiblePost->authorName() }}</a><small>{{ $thread->last_post_at?->diffForHumans() }}</small>@endif</div></article>@empty<div class="profile-panel forum-empty"><h2>No hay temas todavía</h2><p>Inicia la primera conversación cuando quieras.</p></div>@endforelse</section>{{ $threads->links() }}</div></main><x-footer /></body></html>
