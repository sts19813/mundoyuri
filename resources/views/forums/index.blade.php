<!DOCTYPE html>
<html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Foros · Mundo Yuri</title><x-portal-favicon /><link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('assets/css/style.css') }}?v={{ filemtime(public_path('assets/css/style.css')) }}"></head>
<body><x-navbar /><main class="portal-profile-page forum-page"><div class="profile-ambient profile-ambient-one"></div><div class="profile-ambient profile-ambient-two"></div><div class="container-xl px-4 position-relative"><nav class="profile-breadcrumb"><a href="{{ route('home') }}">Inicio</a><span>›</span><a href="{{ route('community.index') }}">Comunidad</a><span>›</span><span>Foros</span></nav><header class="forum-hero"><span class="profile-eyebrow">Conversaciones de la comunidad</span><h1>Foros <em>Mundo Yuri</em></h1><p>Un espacio moderno para conversar, recomendar y reencontrarnos.</p><form method="GET" class="forum-search"><input name="q" type="search" value="{{ $search }}" placeholder="Buscar un foro"><button type="submit">Buscar</button></form></header>
@forelse($categories as $category)<section class="forum-category"><header><span>{{ $category->icon ?: '✦' }}</span><div><h2>{{ $category->name }}</h2>@if($category->description)<p>{{ $category->description }}</p>@endif</div></header><div class="forum-list">@foreach($category->forums as $forum)<article class="forum-row"><div class="forum-row-icon">{{ $forum->icon ?: '◌' }}</div><div class="forum-row-main"><h3><a href="{{ route('forums.show', $forum) }}">{{ $forum->name }}</a>@if($forum->is_locked)<span class="forum-state">Solo lectura</span>@endif</h3><p>{{ $forum->description }}</p></div><dl><div><dt>Temas</dt><dd>{{ number_format($forum->visible_threads_count) }}</dd></div></dl><div class="forum-row-last">@if($forum->latestVisibleThread)<span>Último tema</span><a href="{{ $forum->latestVisibleThread->isQuestion() ? route('questions.show', $forum->latestVisibleThread) : route('forum.threads.show', $forum->latestVisibleThread) }}">{{ $forum->latestVisibleThread->title }}</a><small>{{ $forum->latestVisibleThread->latestVisiblePost?->authorName() ?: $forum->latestVisibleThread->authorName() }} · {{ optional($forum->latestVisibleThread->last_post_at)->diffForHumans() }}</small>@else<span>Sin conversaciones todavía</span>@endif</div></article>@endforeach</div></section>@empty<div class="profile-panel forum-empty"><h2>Aún no hay foros disponibles</h2><p>Vuelve pronto para iniciar una conversación.</p></div>@endforelse</div></main><x-footer /></body></html>
