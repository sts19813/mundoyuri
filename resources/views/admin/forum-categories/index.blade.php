@extends('layouts.admin')
@section('title', 'Categorías de foros - Admin')
@section('toolbar')<h1 class="page-heading text-gray-900 fw-bold fs-3 my-0">Categorías de foros</h1><a href="{{ route('admin.forum-categories.create') }}" class="btn btn-sm btn-primary">Nueva categoría</a>@endsection
@section('content')<div class="card"><div class="card-body"><table class="table align-middle"><thead><tr><th>Nombre</th><th>Foros</th><th>Orden</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($categories as $category)<tr><td><strong>{{ $category->icon }} {{ $category->name }}</strong><div class="text-muted fs-7">{{ $category->description }}</div></td><td>{{ $category->forums_count }}</td><td>{{ $category->sort_order }}</td><td>{{ $category->is_active ? 'Activa' : 'Oculta' }}</td><td class="text-end"><a href="{{ route('admin.forums.create', ['forum_category_id' => $category->id]) }}" class="btn btn-sm btn-light">Nuevo foro</a><a href="{{ route('admin.forum-categories.edit', $category) }}" class="btn btn-sm btn-light-primary">Editar</a>@if($category->forums_count === 0)<form method="POST" action="{{ route('admin.forum-categories.destroy', $category) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-light-danger" onclick="return confirm('¿Eliminar esta categoría vacía?')">Eliminar</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">No hay categorías.</td></tr>@endforelse</tbody></table>{{ $categories->links() }}</div></div>@endsection
