@extends('layouts.admin')
@section('title', 'Foros - Admin')
@section('toolbar')<h1 class="page-heading text-gray-900 fw-bold fs-3 my-0">Foros</h1><a href="{{ route('admin.forums.create') }}" class="btn btn-sm btn-primary">Nuevo foro</a>@endsection
@section('content')<div class="card"><div class="card-body"><table class="table align-middle"><thead><tr><th>Foro</th><th>Categoría</th><th>Temas</th><th>Estado</th><th>Acceso</th><th></th></tr></thead><tbody>@forelse($forums as $forum)<tr><td><strong>{{ $forum->icon }} {{ $forum->name }}</strong><div class="text-muted fs-7">{{ $forum->description }}</div></td><td>{{ $forum->category->name }}</td><td>{{ $forum->threads_count }}</td><td><span class="badge {{ $forum->is_active ? 'badge-light-success' : 'badge-light-warning' }}">{{ $forum->is_active ? 'Activo' : 'Inactivo' }}</span></td><td>{{ $forum->is_locked ? 'Cerrado' : 'Abierto' }} · {{ $forum->minimum_role ?: 'Miembros' }}</td><td class="text-end"><a href="{{ route('admin.forums.edit', $forum) }}" class="btn btn-sm btn-light-primary">Editar</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted">No hay foros.</td></tr>@endforelse</tbody></table>{{ $forums->links() }}</div></div>@endsection
